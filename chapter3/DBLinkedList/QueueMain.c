#include <stdio.h>
#include <stdbool.h>
#include "Queue.h"

int main(void) {
	/*
    Queue* queue;
	int data;
	int length;
	int value;
    
	queue = NULL;
	queue = queue_new();
	length = size(queue);
	printf("size:%d\n", length);

	data = 1;		    
	push(queue, data);
	data = 2;
	push(queue, data);
	data = 3;
	push(queue, data);
	data = 4;
	push(queue, data);
	data = 5;
	push(queue, data);
	length = size(queue);
	printf("size:%d\n", length);

	while (empty(queue)) {
		pop(queue, &value);
		printf("value:%d\n", value);
	}   
	length = size(queue);
	printf("size:%d\n", length);
	data = 5;
	push(queue, data);
	while (empty(queue)) {
		pop(queue, &value);
		printf("value:%d\n", value);
	}
	length = size(queue);
	printf("size:%d\n", length);
	queue_free(queue);*/
	return 0; 
}

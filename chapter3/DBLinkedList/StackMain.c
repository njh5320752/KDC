#include <stdio.h>
#include <stdbool.h>
#include "Stack.h"

int main(void) {
	/*
    Stack* stack;
    int data;
    int value;
	int length;
    
	stack = stack_new();
	length = size(stack);
	printf("size:%d\n", length);
    
	data = 1;
	push(stack, data);
    data = 3;
	push(stack, data);
    data = 5;
	push(stack, data);
    data = 7;
	push(stack, data);
    data = 9;
	push(stack, data);
	length = size(stack);
	printf("size:%d\n", length);

	while (empty(stack)) {
		pop(stack, &value);
		printf("value:%d\n", value);
	}
	data = 9;
	push(stack, data);
	data = 7;
	push(stack, data);
	data = 5;
	push(stack, data);
	data = 3;
	push(stack, data);
	data = 1;
	push(stack, data);

	printf("size:%d\n", length);
	while (empty(stack)) {
		pop(stack, &value);
		printf("value:%d\n", value);
	}
	length = size(stack);
	data = 9;
	push(stack, data);
	printf("size:%d\n", length);
	stack_free(stack); */
	return 0; 
}
